# Flask web framework
Flask>=3.0.0

# YouTube transcript dependencies
youtube-transcript-api>=1.2.2

# Environment variable management
python-dotenv>=1.0.0

# Production WSGI server
gunicorn>=21.0.0

# Thread-based concurrency (no eventlet monkey patching)
# eventlet>=0.33.0  # Removed - causes DNS issues in Termux

# Standard library dependencies (included with Python)
# os - built-in  
# json - built-in
# re - built-in
# time - built-in
# datetime - built-in
# concurrent.futures - built-in
# threading - built-in
# queue - built-in
# urllib.parse - built-in
